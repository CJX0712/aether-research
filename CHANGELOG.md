# 变更日志

本项目遵循 [Semantic Versioning](https://semver.org/)。

## 0.1.0 — 2026-09-18

首个可用版本。

### 核心

- **三条不变量**写进 `src/types.ts`，并由 `Ledger` 在写入路径上强制执行：
  - Claim 只持 `evidenceIds`，永不内联引文文本
  - 无证据的 Claim 不得入库（第一 KPI：不可验证声明数 = 0）
  - 上下文压缩只丢弃整条 Evidence，关联的 Claim 一并丢弃
- **引用编号在渲染期生成**（`Claim → Evidence → Ref` join），存储期不存在"第 N 条引用"，因此并行与压缩都不会让它漂移

### 防幻觉

- 引文逐字校验（`src/synthesis/quote.ts`）：归一化子串匹配 → `direct` + 字符偏移；未命中则吸附到最相似原句 → `inferred`；相似度 < 0.45 丢弃
- 两级空白归一化（折叠 / 完全删除），后者专门救 PDF 与网页复制出的中文换行
- 小数点保护正则：`12.4` 不会被劈成两句
- 按信息量断句（`cjk × 2 + 非 cjk`），避免按字符数过滤误杀短中文句
- 模型编造 `evidenceId` 时被过滤并记录违规

### 流水线

- 七阶段：规划 → 检索/抓取/抽取 → 声明合成 → 冲突裁决 → 大纲 → 逐节生成 → 审计
- 预算在**每阶段开始前**检查，不是跑完统计；触及上限停止开新检索但跑完已启动阶段
- 同站配额（默认 2）+ 空壳页阈值（正文 < 120 字符丢弃）
- SimHash 近似去重，识别转载与镜像站
- 独有词加权归因（DF 逆频权重），避免证据全堆到第一个子问题
- 大纲锚定 + 分段生成，每节只注入本节相关声明
- 冲突不抹平：保留矛盾双方并标注 `contestedWith`，按来源性质/时间/口径裁决
- 规划或大纲阶段模型失效时降级为启发式，保证覆盖全部子问题

### 检索

- 8 个检索源：arXiv / OpenAlex / Crossref（免凭证）、Brave / 博查 / Tavily / Exa / SearXNG
- 多源融合：跨源命中的 URL 排前，论文与官方来源加权，UGC 降权
- 身份判定忽略 http/https 差异（抓取仍用原始 URL，http-only 站点不被升级）
- 免费源走带 429 重试的 `fetchJson`，UA 带 `mailto` 进 polite pool

### 产出

- Markdown / 单文件 HTML（零外部依赖，图标内联 SVG）/ JSON
- 审计区输出：不可验证声明数、冲突声明数、引用来源占比、死链、逐条告警

### 工程

- 41 个测试，全部离线可跑，无需任何 API key
- mock 按 `metadata.label` 路由而非调用序号 —— 用序号的 mock 在流水线多调一次模型时（比如触发解析修复回路）会让后续断言全部错位
- CI：类型检查 + 测试 + 评测集 + 构建 + CLI 冒烟 + emoji 门禁

### 评测集（`npm run eval`，见 `EVAL.md`）

离线、可复现、非循环的防幻觉主线指标：

- 逐字保真率：最终入库的每条证据在夹具原文（去空白）中应是子串 —— 100%
- 幻觉捕获率：喂全编造引文 → 一条证据都不入库；喂改写引文 → 吸附成原文句
- 第一 KPI：不可验证声明数 = 0
- 子问题覆盖率 100%、降级可用性、去重有效性、引用编号完整性

设计原则：**宁可空报告，不要假报告**；失效降级而非报错退出。
