# 评测集

`eval/scorecard.test.ts`（`npm run eval`）。

离线、可复现、且**非循环**：模型是脚本化的，所以"召回率"无法诚实测量——但"最终落库的引文是否 100% 逐字来自原文"可以测，而且这正是该守住的地方。我们故意往流水线里喂编造引文，再检查 Ledger 里最终落下的东西。

## 指标

| 指标 | 如何测 | 当前结果 |
|------|--------|----------|
| **逐字保真率** | 最终入库的每条证据，在夹具原文（去空白）中应是子串 | 100% |
| **幻觉捕获率** | 喂全编造引文 → 一条证据都不入库；喂改写引文 → 吸附成原文句 | 0 命中 / 全吸附 |
| **第一 KPI** | 不可验证声明数 = 0 | 0 |
| **子问题覆盖率** | 大纲覆盖全部规划子问题 | 100% |
| **降级可用性** | 任意阶段模型失效：有兜底（plan/outline）则仍覆盖全部子问题；无材料（extract/claims/compose）则诚实报 `insufficient_sources`，绝不崩溃或编造 | 通过 |
| **去重有效性** | 同源转载不膨胀来源列表，refs 不超过命中 doc 数 | 通过 |
| **引用编号完整性** | Markdown 每个编号都能在来源列表找到，最大值不超 refsUsed | 通过 |

## 设计原则

- **宁可空报告，不要假报告。** `quoteMode: "fabricated"` 下模型全程给假引文，系统产出 `claimsTotal = 0` 而不是把假引文编进报告。这正是第一 KPI 的边界条件。
- **失效降级而非报错退出。** 规划/大纲有启发式兜底；抽取阶段若全盘失效，没有可核验材料就如实报 `insufficient_sources`，不会为了"看起来完整"而塞无证据声明。
- **引用编号在渲染期生成**，所以并行执行与上下文压缩都不会让它漂移——评测里验证每个编号都能对回来源。

## 诚实的局限

- 这套评测度量的是**保真度与覆盖**，不是**召回率**。后者需要真实联网 + 人工标注的"应有声明"语料，且需要模型 API key，不在离线可复现范围。
- 路线图中"带人工标注参照集的召回率评测"是下一步，待真实运行数据积累后补。
